# Программа получает на вход натуральное число N. 
# Нужно найти сумму его делителей. 


def div(a):
    return 1 if a==1 else sum(a//i+i for i in range(1, int(a**0.5)+1) if a%i==0)


print(div(1))   # 1
print(div(10))  # 18
print(div(31))  # 32
